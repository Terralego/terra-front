import React from 'react';

import { storiesOf } from '@storybook/react';

const stories = storiesOf('Apps/Admin', module);

stories.add('Wait for it', () => (
  <p>Some day, an app Admin will raise</p>
));
